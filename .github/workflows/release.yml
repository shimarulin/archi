name: Release

on:
  push:
    branches: [ main ]
    tags-ignore:
    - "*"

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    # https://stackoverflow.com/questions/57921401/push-to-origin-from-github-action/58393457#58393457
    - name: Setup Git
      run: |
        git config --global user.name 'github-actions[bot]'
        git config --global user.email 'shimarulin@users.noreply.github.com'

    - name: CWD
      run: echo $PWD

    - name: Install dependencies
      run: cargo install cocogitto cargo-bump

    - name: Check commits
      run: cog check

    - name: Create release
      run: cog bump --auto

    - name: Deploy release
      uses: softprops/action-gh-release@v1
      if: startsWith(github.ref, 'refs/tags/')
      with:
        files: target/release/archi
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
